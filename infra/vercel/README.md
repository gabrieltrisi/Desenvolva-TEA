# infra/vercel

**Infraestrutura do frontend na Vercel.**

> ⚠️ **Placeholder.** Nenhum projeto Vercel foi conectado nem arquivo de infra
> ativo foi adicionado.

## Objetivo

Guardar, no futuro, a configuração de deploy de [`apps/web`](../../apps/web/README.md)
na **Vercel**:

- `vercel.json` (se necessário): rewrites, headers, regiões.
- Variáveis de ambiente públicas (`NEXT_PUBLIC_SITE_URL`,
  `NEXT_PUBLIC_API_URL` apontando para a Render).
- Configuração de build/Root Directory quando o app virar `apps/web`.

## Por enquanto

Apenas documentação. O passo a passo está em
[`docs/VERCEL_DEPLOYMENT.md`](../../docs/VERCEL_DEPLOYMENT.md).
